# Smart Spend - .env.template
# Copy to .env and fill with real values for development/production.
# This template lists the variables the application reads at runtime. Do
# NOT commit your filled `.env` file — keep secrets out of source control.

# === AUTH / SECURITY ===
# A long, random secret used to sign JWT tokens. Use a secure secret in
# production and rotate if exposed. Minimum 32+ characters recommended.
SECRET_KEY=replace-with-a-very-long-random-secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
ENV=development

# === DATABASE ===
# SQLAlchemy async URL. Example (docker-compose uses service name `postgres`):
# postgresql+asyncpg://<POSTGRES_USER>:<POSTGRES_PASSWORD>@postgres:5432/<POSTGRES_DB>
# When running under Docker Compose prefer the service hostname `postgres`.
# Use a placeholder here and update your local `.env` with real secrets.
DATABASE_URL=postgresql+asyncpg://postgres:replace-with-password@postgres:5432/smart_spend

# === REDIS (ARQ Worker) ===
# When running with docker-compose use REDIS_HOST=redis so containers can
# resolve the service name. For local development you can use 127.0.0.1.
# Default to `redis` to make the template Compose-friendly; replace with
# 127.0.0.1 when running a local Redis instance on the host.
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=replace-with-redis-password-or-empty

# === HUGGINGFACE / AI ===
# API token for HuggingFace (if you use HF-hosted models)
HF_TOKEN=

# === OPTIONAL (container helper variables)
# These are useful when running Postgres as a container (docker-compose).
# They are not required by the app directly if `DATABASE_URL` is set.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace-with-postgres-password
POSTGRES_DB=smart_spend
POSTGRES_PORT=5432

# Adminer (optional)
ADMINER_PORT=8080
